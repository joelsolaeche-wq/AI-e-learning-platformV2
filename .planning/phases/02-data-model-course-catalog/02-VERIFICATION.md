---
phase: 02
status: human_needed
requirements_verified:
  - CATALOG-01
  - CATALOG-02
must_haves_score: 19/20
verified_at: 2026-04-28
---

# Verification: Phase 02 — Data Model + Course Catalog

## Goal Check

The phase goal was substantially achieved in code and artifacts: all 12 tables exist in the migration with RLS enabled, seed data is complete and correctly structured, the catalog and detail pages are implemented as Server Components with auth guards, and middleware protects the `/catalog` route. One must-have (npm run build exits 0) was reported as partially blocked by a TypeScript issue in `app/catalog/page.tsx`, but the task prompt confirms the build is now passing (all routes compiled), so this is resolved.

## Success Criteria

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | 12 tables + RLS | ✓ | Migration 000002 defines 11 tables with `enable row level security` (11 occurrences confirmed via grep). Migration 000001 adds RLS to `profiles`. All 12 tables present in `lib/database.types.ts` (confirmed: `ai_chat_messages`, `ai_chat_sessions`, `cohorts`, `courses`, `enrollments`, `lesson_progress`, `lessons`, `modules`, `organizations`, `profiles`, `quiz_attempts`, `quiz_definitions`). `fk_profiles_org` constraint present in migration file. |
| 2 | Catalog page renders courses | ✓ | `app/catalog/page.tsx` is a Server Component (no `"use client"`), queries `courses` with `.eq('is_published', true)`, renders cards with `aspect-video` thumbnail, title, `line-clamp-2` description, `Badge` variant="secondary" with "AI", and "View Course" `Button` linking to `/catalog/${course.id}`. 3-column grid with `lg:grid-cols-3` confirmed. |
| 3 | Course detail page works | ✓ | `app/catalog/[courseId]/page.tsx` is a Server Component, uses `params: Promise<{ courseId: string }>` (Next.js 15), `await params`, `getUser()` auth check, `notFound()` on missing course, `Promise.all` for 3 parallel queries (courses/modules/cohorts). Shows Course Outline (module position + lesson duration in minutes) and Available Cohorts (status badge, disabled "Join Cohort" button). `Separator` used between sections. |
| 4 | Seed data populated | ✓ | `supabase/seed.sql` contains: 1 org (Taller Technologies), 1 published course (Generative AI Fundamentals, `is_published = true`, picsum thumbnail), 3 modules (positions 1-3), 3 lessons (one per module, duration_seconds 600/720/840), 1 quiz definition (3 questions with correct_answer), 1 active cohort (May 2026 Cohort, starts 2026-05-01). All inserts use fixed UUIDs and `ON CONFLICT (id) DO NOTHING`. No user-dependent tables seeded. 02-02-SUMMARY confirms seed ran successfully via `npx supabase db query --linked -f supabase/seed.sql`. |
| 5 | Auth redirect for /catalog | ✓ | `middleware.ts` line 34: `const protectedPaths = ['/dashboard', '/cohorts', '/lessons', '/admin', '/catalog']`. Uses `pathname.startsWith(p)` so `/catalog/[courseId]` is also protected. Redirects to `/auth/login?redirectTo=<pathname>`. Both catalog pages additionally call `getUser()` and `redirect('/auth/login')` as belt-and-suspenders. |

## Must-Haves Check

**02-01 must_haves:**
- [x] `supabase/migrations/20260428000002_create_remaining_tables.sql` exists — confirmed, file read in full
- [x] Contains all 11 tables + RLS policies + profiles FK — confirmed via file read and grep (11 RLS statements, `fk_profiles_org` present)
- [x] `npx supabase db push` completed — 02-01-SUMMARY reports tasks 2-01-02 and 2-01-03 as complete
- [x] `npx supabase migration list` shows 000002 as applied — reported in SUMMARY
- [x] All 12 tables have `rowsecurity = true` — migration file enables RLS on all 11 new tables; profiles has RLS from migration 000001
- [x] `fk_profiles_org` constraint exists — `alter table public.profiles add constraint fk_profiles_org` confirmed in migration file

**02-02 must_haves:**
- [x] `supabase/seed.sql` exists with all required data — confirmed, file read in full (org, course, 3 modules, 3 lessons, quiz def, cohort)
- [x] Seed SQL ran without FK violations — 02-02-SUMMARY confirms successful run
- [x] `lib/database.types.ts` generated by `npx supabase gen types typescript --linked` — confirmed, file present with all 12 tables (610 lines per SUMMARY)
- [x] `lib/supabase/server.ts` uses `createServerClient<Database>` — confirmed in code
- [x] `lib/supabase/client.ts` uses `createBrowserClient<Database>` — confirmed in code
- [x] `npm run build` exits 0 — confirmed by task prompt (build is passing)

**02-03 must_haves:**
- [x] `middleware.ts` `protectedPaths` includes `'/catalog'` — confirmed at line 34
- [x] `components/ui/badge.tsx` exists — confirmed via glob
- [x] `components/ui/skeleton.tsx` exists — confirmed via glob
- [x] `components/ui/separator.tsx` exists — confirmed via glob
- [x] Unauthenticated redirect to `/auth/login?redirectTo=/catalog` — code confirmed; actual redirect behavior requires human test

**02-04 must_haves:**
- [x] `app/catalog/page.tsx` exists as Server Component — confirmed, no `"use client"`
- [x] `getUser()` + redirect to `/auth/login` — confirmed
- [x] Queries `courses` with `is_published: true` — confirmed
- [x] Cards render thumbnail (aspect-video), title, description (line-clamp-2), Badge ("AI"), "View Course" button — confirmed
- [x] "View Course" links to `/catalog/${course.id}` — confirmed
- [x] Empty state — confirmed (`No courses available yet`)
- [x] Error state — confirmed (`Failed to load courses. Refresh the page to try again.`)
- [x] `npm run build` exits 0 — confirmed per task prompt
- NOTE: Deviation from spec — `cohorts (count)` removed from SELECT query (TypeScript inference issue); functionally correct since count was not rendered in the UI

**02-05 must_haves:**
- [x] `app/catalog/[courseId]/page.tsx` exists as Server Component — confirmed, no `"use client"`
- [x] `params: Promise<{ courseId: string }>` (Next.js 15) — confirmed
- [x] `getUser()` + redirect to `/auth/login` — confirmed
- [x] `notFound()` for missing course — confirmed
- [x] Three parallel queries via `Promise.all` — confirmed
- [x] Course outline section with module positions and lesson durations — confirmed (`Math.round(lesson.duration_seconds / 60)` present)
- [x] Cohort schedule with status badge and disabled "Join Cohort" — confirmed
- [x] `Separator` between sections — confirmed (3 Separator usages)
- [x] `npm run build` exits 0 — confirmed per task prompt (build passing)
- NOTE: Deviation — explicit TypeScript types (`CourseRow`, `ModuleWithLessons`, `LessonRow`, `CohortRow`) added to work around Supabase discriminated union narrowing; functionally equivalent to spec

**Score: 19/20** — All must-haves pass. The one partial flag was `npm run build` in 02-05-SUMMARY, which the task prompt confirms is now resolved.

## Requirement Traceability

| Requirement | Status | Evidence |
|-------------|--------|----------|
| CATALOG-01 | ✓ | 12 tables in migration with RLS (11 in 000002 + profiles in 000001), TypeScript types generated (`lib/database.types.ts`, all 12 tables confirmed), seed data present with correct FK ordering and idempotent inserts, both Supabase clients typed with `Database` generic |
| CATALOG-02 | ✓ | `/catalog` page at `app/catalog/page.tsx` — Server Component, auth-guarded, renders published course cards with all required UI elements. `/catalog/[courseId]` page at `app/catalog/[courseId]/page.tsx` — Server Component, auth-guarded, 404 on missing courses, parallel queries, full module/lesson outline, cohort schedule with disabled enrollment CTA |

## Human Verification Items

The following items cannot be verified from static code analysis alone — they require running the application:

1. **Visual rendering of catalog page** — Navigate to `http://localhost:3000/catalog` when signed in. Verify: picsum.photos thumbnail loads at 16:9 ratio, "Generative AI Fundamentals" card title is visible, description is 2-line-clamped, "AI" badge appears, "View Course" button is clickable.

2. **Visual rendering of course detail page** — Click "View Course" → navigate to `/catalog/00000000-0000-0000-0000-000000000010`. Verify: thumbnail renders, 3 modules show with labels "Module 1/2/3", lessons appear with durations (10 min, 12 min, 14 min), "May 2026 Cohort" card shows status badge "active", "May 1, 2026" start date, "20 seats available", and a visually disabled "Join Cohort" button.

3. **Auth redirect behavior** — Sign out, then navigate to `http://localhost:3000/catalog`. Verify: browser URL changes to `http://localhost:3000/auth/login?redirectTo=%2Fcatalog` (or `/auth/login?redirectTo=/catalog`).

4. **404 behavior** — While signed in, navigate to `/catalog/00000000-0000-0000-0000-999999999999`. Verify: Next.js 404 page is displayed, not a blank page or error boundary.

5. **Supabase DB state confirmation** — Run in Supabase SQL editor to confirm live DB matches seed file:
   ```sql
   select count(*) from public.courses where is_published = true;      -- expect 1
   select count(*) from public.modules;                                  -- expect 3
   select count(*) from public.lessons;                                  -- expect 3
   select count(*) from public.cohorts where status = 'active';         -- expect 1
   select count(*) from public.quiz_definitions;                         -- expect 1
   select tablename, rowsecurity from pg_tables
     where schemaname = 'public'
     and tablename in ('profiles','organizations','courses','modules','lessons',
       'quiz_definitions','cohorts','enrollments','lesson_progress',
       'quiz_attempts','ai_chat_sessions','ai_chat_messages')
     order by tablename;                                                   -- expect 12 rows, all rowsecurity=true
   ```

6. **RLS anon block** — Run `set role anon; select * from public.courses; reset role;` in Supabase SQL editor — expect 0 rows returned.

## Gaps

None — all code-verifiable criteria pass. The 02-05-SUMMARY noted a build issue that the task prompt confirms is resolved. The deviations (removing `cohorts (count)` from catalog query; adding explicit TypeScript type aliases in detail page) are functionally sound and do not affect any success criterion.
