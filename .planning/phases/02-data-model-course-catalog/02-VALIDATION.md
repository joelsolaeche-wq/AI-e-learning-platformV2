---
phase: 2
slug: data-model-course-catalog
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-28
---

# Phase 2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | TypeScript compiler + Next.js build + manual browser |
| **Config file** | `tsconfig.json` / `next.config.ts` |
| **Quick run command** | `npm run build` |
| **Full suite command** | `npm run build && npx supabase migration list` |
| **Estimated runtime** | ~30 seconds |

No unit test framework is installed in this project. Phase 2 verification relies on TypeScript compilation (catching type errors from generated DB types), Supabase CLI migration status, and manual browser verification of the catalog UI.

---

## Sampling Rate

- **After every task commit:** Run `npm run build` (TypeScript must exit 0)
- **After every plan wave:** Run `npm run build && npx supabase migration list`
- **Before `/gsd-verify-work`:** Full build green + all SQL verification queries pass
- **Max feedback latency:** 30 seconds (build) + manual steps

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 2-01-01 | 01 | 1 | CATALOG-01 | — | Migration file present, schema valid SQL | build | `npm run build` | ❌ W0 | ⬜ pending |
| 2-01-02 | 01 | 1 | CATALOG-01 | — | `npx supabase db push` exits 0 | manual | `npx supabase db push` | ❌ W0 | ⬜ pending |
| 2-01-03 | 01 | 1 | CATALOG-01 | — | Anon cannot read courses (RLS) | manual-sql | SQL: `set role anon; select * from public.courses;` → 0 rows | — | ⬜ pending |
| 2-02-01 | 02 | 1 | CATALOG-01 | — | Types compile after `gen types` | build | `npm run build` | ❌ W0 | ⬜ pending |
| 2-02-02 | 02 | 1 | CATALOG-01 | — | seed.sql runs without FK errors | manual-sql | paste seed.sql in Supabase SQL editor | — | ⬜ pending |
| 2-03-01 | 03 | 2 | CATALOG-01 | — | /catalog middleware redirects anon | manual-browser | sign out → navigate to /catalog → lands on /auth/login | — | ⬜ pending |
| 2-03-02 | 03 | 2 | CATALOG-01 | — | /catalog shows course cards | manual-browser | sign in → navigate to /catalog → card grid visible | — | ⬜ pending |
| 2-05-01 | 05 | 3 | CATALOG-02 | — | /catalog/[courseId] shows outline + cohort | manual-browser | click "View Course" → see modules + cohort card | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] No new test files to create — project has no test framework
- [ ] `lib/database.types.ts` — generated by `npx supabase gen types typescript --linked`

*TypeScript build is the primary automated check. All schema and UI verification is manual for this phase.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| All 12 tables present with RLS | CATALOG-01 | SQL query required | Run in Supabase SQL editor: `select tablename, rowsecurity from pg_tables where schemaname = 'public'` — verify 12 rows, all `rowsecurity = true` |
| Anon user blocked from /catalog | CATALOG-01 | Browser session required | Sign out → navigate to /catalog → confirm redirect to /auth/login |
| Course card grid renders | CATALOG-01 | Browser rendering | Sign in → /catalog → verify 3-col grid, thumbnail, title, description, "View Course" button |
| Course detail shows outline | CATALOG-02 | Browser rendering | Click any course card → /catalog/[id] → verify module list with lessons and cohort section |
| Seed data visible | CATALOG-01 | DB state verification | SQL: `select title from public.courses` → 1 row; `select count(*) from public.modules` → 3 |
| FK profiles.org_id → organizations | CATALOG-01 | DB constraint check | SQL: `select constraint_name from information_schema.table_constraints where table_name = 'profiles' and constraint_type = 'FOREIGN KEY'` → includes `fk_profiles_org` |

---

## Validation Sign-Off

- [ ] All tasks have automated verify (npm run build) or documented manual steps
- [ ] Sampling continuity: TypeScript build runs after every task
- [ ] Wave 0: lib/database.types.ts generated before catalog pages are built
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (TypeScript build)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
